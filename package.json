{
  "name": "vecn",
  "version": "0.1.0",
  "description": "A module for creating n-dimensional vector types that support swizzling.",
  "main": "src/index.js",
  "scripts": {
    "test": "mocha -u tdd",
    "docs": "jsdoc src -d docs -P ./package.json -R ./README.md && rsync -a --remove-source-files docs/**/**/* docs/ && find docs -type d -empty -delete"
  },
  "author": "Bryce Wilson",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Zunawe/vecn"
  },
  "devDependencies": {
    "mocha": "^5.1.1"
  },
  "dependencies": {
    "assert": "^1.4.1"
  }
}
